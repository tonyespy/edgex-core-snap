name: edgexfoundry-core
version: '0.2.1+barca-4'
summary: Provides a full set of Edgex Foundry core microservices.
description: |
 This snap provides a full set of EdgeX Foundry core micro services.
 It's based on the 'Barcalona' release of EdgeX, in which most of
 the micro services are written in Java.  The notable exceptions
 are Consul and Mongodb. This snap includes a shared OpenJDK JRE
 for use by all of the services. The packaging for this snap can
 be found at:
 https://github.com/tonyespy/edgex-core-snap

grade: stable
confinement: strict
#
# TODO: still a few bugs to work out for confinement;
# seems that there are apparmor denials being generated
# for open calls to the directory: /var/lib/snapd/void/.
# Prior to the changes to the start-edgex script, each
# service's log files were being written here, so not
# sure why denials are still being generated.  This
# be caused by the fact that the current working dir
# may be defaulting to this dir...

apps:
  consul:
    command: bin/start-consul.sh
    # TODO: add stop-command
    plugs: [network, network-bind]
  core-config-seed:
    command: bin/start-config-seed.sh
    plugs: [network, network-bind]
  start-edgex:
    command: bin/start-edgex.sh
    plugs: [network, network-bind]
  stop-edgex:
    command: bin/stop-edgex.sh
  mongo:
    command: bin/mongo
    plugs: [network]
  mongod:
    command: mongo/launch-edgex-mongo.sh
    plugs: [hardware-observe, network, network-bind, system-observe]
  mongostat:
    command: bin/mongostat
    plugs: [network]
  mongodump:
    command: bin/mongodump
    plugs: [network]
  consul-cli:
    command: bin/consul
    plugs: [network, network-bind]

parts:
  curl:
    plugin: nil
    stage-packages:
      - curl
  go:
    source-tag: go1.9.2
  consul:
    plugin: shell
    source: https://github.com/hashicorp/consul.git
    # source: ./consul
    source-tag: v0.7.3
    after: [go]
    prepare: git apply $SNAPCRAFT_STAGE/0001-Link-all-dependencies-in-vendor-folder-to-the-custom.patch
    shell: bash
    shell-flags: ['-eux', '-o', 'pipefail']
    shell-command: |
      export GOROOT="$SNAPDIR/parts/go/install"
      export PATH="$GOROOT/bin:$PATH"

      export XC_ARCH=`dpkg --print-architecture`
      export XC_OS="linux"

      go version

      mkdir -p .gopath/src/github.com/hashicorp
      export GOPATH="$PWD/.gopath"
      export PATH="$GOPATH/bin:$PATH"
      ln -s "$PWD" .gopath/src/github.com/hashicorp/consul

      pushd .gopath/src/github.com/hashicorp/consul
      make bin
      popd

      consulBin=".gopath/src/github.com/hashicorp/consul/pkg/linux_$XC_ARCH/consul"
      "$consulBin" -v
      install -DT "$(readlink -f "$consulBin")" "$SNAPCRAFT_PART_INSTALL/bin/consul"
      # handle consul LICENSE
      # TODO: do PATENT files need copying?
      install -DT ".gopath/src/github.com/hashicorp/consul/LICENSE" \
                 "$SNAPCRAFT_PART_INSTALL/usr/share/doc/github.com/hashicorp/consul/LICENSE"

      # handle vendor LICENSE files
      pushd vendor
      for i in `find . -type f -name "LICENSE"`; do
        install -DT "$i" \
                 "$SNAPCRAFT_PART_INSTALL/usr/share/doc/$i"; done
      popd

      # TODO: some LICENSE files fall under .gopath too
      pushd .gopath/src
      for i in `find . -type f -name "LICENSE"`; do
        install -DT "$i" \
                 "$SNAPCRAFT_PART_INSTALL/usr/share/doc/$i"; done
      popd
    after:
      - go
      - patch
    build-packages:
      - make
      - zip
  patch:
    source: patches
    plugin: dump
    prime:
      - -*
  config-seed:
    source: https://github.com/edgexfoundry/core-config-seed.git
    source-branch: barcelona
    plugin: maven
    install: |
      echo "Installing config files"

      # The logic following logic is all handled by DockerFile for
      # the EdgeX core-config-seed docker image.
      install -d "$SNAPCRAFT_PART_INSTALL/jar/config-seed"
      mv "$SNAPCRAFT_PART_INSTALL/jar/core-config-seed-0.2.1-SNAPSHOT.jar" \
         "$SNAPCRAFT_PART_INSTALL/jar/config-seed/core-config-seed.jar"

      # FIXME:
      # copy config-seed license into /usr/share/java/doc, because the
      # jdk plugin has a bug which prevents any files from /usr/share/doc
      # to be staged or primed.
      install -DT "./Attribution.txt" \
                 "$SNAPCRAFT_PART_INSTALL/usr/share/java/doc/core-config-seed/Attribution.txt"
      install -DT "./LICENSE-2.0.txt" \
                 "$SNAPCRAFT_PART_INSTALL/usr/share/java/doc/core-config-seed/LICENSE-2.0.txt"

      for i in `ls ./config`; do
        install -DT "./config/$i/application.properties" \
           "$SNAPCRAFT_PART_INSTALL/jar/config-seed/config/$i/application.properties"; done
    build-packages:
      - ca-certificates-java

    # Since snapcraft jdk plugin blanket blacklists usr/share/doc from stage,
    # move JRE changelog & copyright to usr/share/java/doc
    organize:
      usr/share/doc/openjdk-8-jre-headless/changelog.Debian.gz: usr/share/java/doc/openjdk-8-jre-headless/changelog.Debian.gz
      usr/share/doc/openjdk-8-jre-headless/copyright: usr/share/java/doc/openjdk-8-jre-headless/copyright

    # TODO (applies to all edgex parts):
    #   * a custom plugin could simplify the duplication of prime rules across
    #     EdgeX service parts
    #   * remove remaining license files from:
    #     usr/lib/jvm/java-8-openjdk-`arch`/jre/
    prime:
      - -etc/fonts
      - -etc/fonts/X11
      - -usr/lib/jvm/*/ASSEMBLY_EXCEPTION
      - -usr/lib/jvm/*/THIRD_PARTY_README
      - -usr/lib/jvm/*/jre/ASSEMBLY_EXCEPTION
      - -usr/lib/jvm/*/jre/THIRD_PARTY_README
      - -usr/lib/jvm/*/man
      - -usr/lib/jvm/*/jre/man
      - -usr/lib/jvm/*/jre/lib/images
      - -usr/lib/jvm/*/include
      - -usr/lib/jvm/*/bin
      - -usr/lib/jvm/*/lib
      - -usr/lib/jvm/*/docs
      - -usr/lib/jvm/*/src.zip
      - -usr/share/X11
      - -usr/share/man
      - -usr/share/fonts
      - -usr/share/alsa
  config-common:
    plugin: dump
    source: .
    stage:
      - bin/start-consul.sh
      - bin/start-config-seed.sh
      - bin/start-edgex.sh
      - bin/stop-edgex.sh
      - config/edgex-services-env
  mongodb:
    plugin: shell
    source: .
    shell: bash
    shell-flags: ['-eux', '-o', 'pipefail']
    shell-command: |
      export ARCH=`arch`

      if [ $ARCH = "x86_64" ] ; then
        export PACKAGE="mongodb-linux-x86_64-ubuntu1604-3.4.10.tgz"
      elif [ $ARCH = "aarch64" ] ; then
        export PACKAGE="mongodb-linux-arm64-ubuntu1604-3.4.10.tgz"
      fi

      pushd $SNAPCRAFT_PART_INSTALL
      wget "https://fastdl.mongodb.org/linux/$PACKAGE"
      tar xvf "./$PACKAGE" --strip-components=1
      popd
    organize:
      GNU-AGPL-3.0: usr/share/doc/mongodb/GNU-AGPL-3.0
      MPL-2: usr/share/doc/mongodb/MPL-2
      README: usr/share/doc/mongodb/README
      THIRD-PARTY-NOTICES: usr/share/doc/mongodb/THIRD-PARTY-NOTICES
    stage-packages:
    - libssl1.0.0
    stage:
      - -bin/bsondump
      - -bin/mongoexport
      - -bin/mongofiles
      - -bin/mongoimport
      - -bin/mongooplog
      - -bin/mongoperf
      - -bin/mongoreplay
      - -bin/mongorestore
      - -bin/mongos
      - -bin/mongotop
      - -mongodb-linux-*-ubuntu1604-3.4.10.tgz
  mongo-config:
    source: https://github.com/edgexfoundry/docker-edgex-mongo.git
    plugin: dump
    # NOTE - both the mongo-config & go install README.md to root-dir of the snap
    # which breaks when 'stage' runs.  The only workaround so far is
    # is to not stage these files.  This needs a better solution long-term.
    stage:
      - -README.md
      - -Dockerfile*
    install: |
      echo "Installing mongo-config files"
      echo "DIR=`pwd`"
      echo "SNAPCRAFT_PART_INSTALL=$SNAPCRAFT_PART_INSTALL"
      install -d "$SNAPCRAFT_PART_INSTALL/mongo"
      cat ./launch-edgex-mongo.sh | \
        sed -e s/\$SLEEP_TIME_BEFORE_CONFIG/10/ \
            -e s:mongod:mongod\ --dbpath\ \$SNAP_DATA/mongo/db: \
            -e '/wait/ d' \
            -e s:/edgex/mongo/config/:\$SNAP/mongo/: > \
         "$SNAPCRAFT_PART_INSTALL/mongo/launch-edgex-mongo.sh"
      mv "$SNAPCRAFT_PART_INSTALL/init_mongo.js" \
         "$SNAPCRAFT_PART_INSTALL/mongo/init_mongo.js"
      chmod 755 "$SNAPCRAFT_PART_INSTALL/mongo/launch-edgex-mongo.sh"
      install -DT "./LICENSE-2.0.TXT" \
         "$SNAPCRAFT_PART_INSTALL/usr/share/doc/mongo-config/LICENSE-2.0.TXT"
    prime:
      - -launch-edgex-mongo.sh
      - -LICENSE-2.0.TXT
  core-data:
    source: https://github.com/edgexfoundry/core-data.git
    source-branch: barcelona
    plugin: maven
    install: |
      echo "Installing core-data files"

      # The logic following logic is all handled by DockerFile for
      # the EdgeX core-data docker image.
      install -d "$SNAPCRAFT_PART_INSTALL/jar/core-data"
      mv "$SNAPCRAFT_PART_INSTALL/jar/core-data-0.2.1-SNAPSHOT.jar" \
         "$SNAPCRAFT_PART_INSTALL/jar/core-data/core-data.jar"

      # FIXME:
      # copy service license into /usr/share/java/doc, because the
      # jdk plugin has a bug which prevents any files from /usr/share/doc
      # to be staged or primed.
      install -DT "./Attribution.txt" \
         "$SNAPCRAFT_PART_INSTALL/usr/share/java/doc/core-data/Attribution.txt"
      install -DT "./LICENSE-2.0.txt" \
         "$SNAPCRAFT_PART_INSTALL/usr/share/java/doc/core-data/LICENSE-2.0.txt"
    prime:
      - -etc/fonts
      - -etc/fonts/X11
      - -usr/lib/jvm/*/ASSEMBLY_EXCEPTION
      - -usr/lib/jvm/*/THIRD_PARTY_README
      - -usr/lib/jvm/*/jre/ASSEMBLY_EXCEPTION
      - -usr/lib/jvm/*/jre/THIRD_PARTY_README
      - -usr/lib/jvm/*/man
      - -usr/lib/jvm/*/jre/man
      - -usr/lib/jvm/*/jre/lib/images
      - -usr/lib/jvm/*/include
      - -usr/lib/jvm/*/bin
      - -usr/lib/jvm/*/lib
      - -usr/lib/jvm/*/docs
      - -usr/lib/jvm/*/src.zip
      - -usr/share/X11
      - -usr/share/man
      - -usr/share/fonts
      - -usr/share/alsa
  support-logging:
    source: https://github.com/edgexfoundry/support-logging.git
    source-branch: barcelona
    plugin: maven
    install: |
      echo "Installing support-logging files"

      # The logic following logic is all handled by DockerFile for
      # the EdgeX support-logging docker image.
      install -d "$SNAPCRAFT_PART_INSTALL/jar/support-logging"
      mv "$SNAPCRAFT_PART_INSTALL/jar/support-logging-0.2.1-SNAPSHOT.jar" \
         "$SNAPCRAFT_PART_INSTALL/jar/support-logging/support-logging.jar"

      # FIXME:
      # copy service license into /usr/share/java/doc, because the
      # jdk plugin has a bug which prevents any files from /usr/share/doc
      # to be staged or primed.
      install -DT "./Attribution.txt" \
         "$SNAPCRAFT_PART_INSTALL/usr/share/java/doc/support-logging/Attribution.txt"
      install -DT "./LICENSE-2.0.txt" \
         "$SNAPCRAFT_PART_INSTALL/usr/share/java/doc/support-logging/LICENSE-2.0.txt"
    prime:
      - -etc/fonts
      - -etc/fonts/X11
      - -usr/lib/jvm/*/ASSEMBLY_EXCEPTION
      - -usr/lib/jvm/*/THIRD_PARTY_README
      - -usr/lib/jvm/*/jre/ASSEMBLY_EXCEPTION
      - -usr/lib/jvm/*/jre/THIRD_PARTY_README
      - -usr/lib/jvm/*/man
      - -usr/lib/jvm/*/jre/man
      - -usr/lib/jvm/*/jre/lib/images
      - -usr/lib/jvm/*/include
      - -usr/lib/jvm/*/bin
      - -usr/lib/jvm/*/lib
      - -usr/lib/jvm/*/docs
      - -usr/lib/jvm/*/src.zip
      - -usr/share/X11
      - -usr/share/man
      - -usr/share/fonts
      - -usr/share/alsa
  support-notifications:
    source: https://github.com/edgexfoundry/support-notifications.git
    source-branch: barcelona
    plugin: maven
    install: |
      echo "Installing support-notifications files"

      # The logic following logic is all handled by DockerFile for
      # the EdgeX support-notifications docker image.
      install -d "$SNAPCRAFT_PART_INSTALL/jar/support-notifications"
      mv "$SNAPCRAFT_PART_INSTALL/jar/support-notifications-0.2.1-SNAPSHOT.jar" \
         "$SNAPCRAFT_PART_INSTALL/jar/support-notifications/support-notifications.jar"

      # FIXME:
      # copy service license into /usr/share/java/doc, because the
      # jdk plugin has a bug which prevents any files from /usr/share/doc
      # to be staged or primed.
      install -DT "./Attribution.txt" \
         "$SNAPCRAFT_PART_INSTALL/usr/share/java/doc/support-notifications/Attribution.txt"
      install -DT "./LICENSE-2.0.txt" \
         "$SNAPCRAFT_PART_INSTALL/usr/share/java/doc/support-notifications/LICENSE-2.0.txt"
    prime:
      - -etc/fonts
      - -etc/fonts/X11
      - -usr/lib/jvm/*/ASSEMBLY_EXCEPTION
      - -usr/lib/jvm/*/THIRD_PARTY_README
      - -usr/lib/jvm/*/jre/ASSEMBLY_EXCEPTION
      - -usr/lib/jvm/*/jre/THIRD_PARTY_README
      - -usr/lib/jvm/*/man
      - -usr/lib/jvm/*/jre/man
      - -usr/lib/jvm/*/jre/lib/images
      - -usr/lib/jvm/*/include
      - -usr/lib/jvm/*/bin
      - -usr/lib/jvm/*/lib
      - -usr/lib/jvm/*/docs
      - -usr/lib/jvm/*/src.zip
      - -usr/share/X11
      - -usr/share/man
      - -usr/share/fonts
      - -usr/share/alsa
  core-metadata:
    source: https://github.com/edgexfoundry/core-metadata.git
    source-branch: barcelona
    plugin: maven
    install: |
      echo "Installing core-metadata files"

      # The logic following logic is all handled by DockerFile for
      # the EdgeX core-metadata docker image.
      install -d "$SNAPCRAFT_PART_INSTALL/jar/core-metadata"
      mv "$SNAPCRAFT_PART_INSTALL/jar/core-metadata-0.2.1-SNAPSHOT.jar" \
         "$SNAPCRAFT_PART_INSTALL/jar/core-metadata/core-metadata.jar"

      # FIXME:
      # copy service license into /usr/share/java/doc, because the
      # jdk plugin has a bug which prevents any files from /usr/share/doc
      # to be staged or primed.
      install -DT "./Attribution.txt" \
         "$SNAPCRAFT_PART_INSTALL/usr/share/java/doc/core-metadata/Attribution.txt"
      install -DT "./LICENSE-2.0.txt" \
         "$SNAPCRAFT_PART_INSTALL/usr/share/java/doc/core-metadata/LICENSE-2.0.txt"
    prime:
      - -etc/fonts
      - -etc/fonts/X11
      - -usr/lib/jvm/*/ASSEMBLY_EXCEPTION
      - -usr/lib/jvm/*/THIRD_PARTY_README
      - -usr/lib/jvm/*/jre/ASSEMBLY_EXCEPTION
      - -usr/lib/jvm/*/jre/THIRD_PARTY_README
      - -usr/lib/jvm/*/man
      - -usr/lib/jvm/*/jre/man
      - -usr/lib/jvm/*/jre/lib/images
      - -usr/lib/jvm/*/include
      - -usr/lib/jvm/*/bin
      - -usr/lib/jvm/*/lib
      - -usr/lib/jvm/*/docs
      - -usr/lib/jvm/*/src.zip
      - -usr/share/X11
      - -usr/share/man
      - -usr/share/fonts
      - -usr/share/alsa
  core-command:
    source: https://github.com/edgexfoundry/core-command.git
    source-branch: barcelona
    plugin: maven
    install: |
      echo "Installing core-command files"

      # The logic following logic is all handled by DockerFile for
      # the EdgeX core-command docker image.
      install -d "$SNAPCRAFT_PART_INSTALL/jar/core-command"
      mv "$SNAPCRAFT_PART_INSTALL/jar/core-command-0.2.1-SNAPSHOT.jar" \
         "$SNAPCRAFT_PART_INSTALL/jar/core-command/core-command.jar"

      # FIXME:
      # copy service license into /usr/share/java/doc, because the
      # jdk plugin has a bug which prevents any files from /usr/share/doc
      # to be staged or primed.
      install -DT "./Attribution.txt" \
         "$SNAPCRAFT_PART_INSTALL/usr/share/java/doc/core-command/Attribution.txt"
      install -DT "./LICENSE-2.0.txt" \
         "$SNAPCRAFT_PART_INSTALL/usr/share/java/doc/core-command/LICENSE-2.0.txt"
    prime:
      - -etc/fonts
      - -etc/fonts/X11
      - -usr/lib/jvm/*/ASSEMBLY_EXCEPTION
      - -usr/lib/jvm/*/THIRD_PARTY_README
      - -usr/lib/jvm/*/jre/ASSEMBLY_EXCEPTION
      - -usr/lib/jvm/*/jre/THIRD_PARTY_README
      - -usr/lib/jvm/*/man
      - -usr/lib/jvm/*/jre/man
      - -usr/lib/jvm/*/jre/lib/images
      - -usr/lib/jvm/*/include
      - -usr/lib/jvm/*/bin
      - -usr/lib/jvm/*/lib
      - -usr/lib/jvm/*/docs
      - -usr/lib/jvm/*/src.zip
      - -usr/share/X11
      - -usr/share/man
      - -usr/share/fonts
      - -usr/share/alsa
  support-scheduler:
    source: https://github.com/edgexfoundry/support-scheduler.git
    source-branch: barcelona
    plugin: maven
    install: |
      echo "Installing support-scheduler files"

      # The logic following logic is all handled by DockerFile for
      # the EdgeX support-scheduler docker image.
      install -d "$SNAPCRAFT_PART_INSTALL/jar/support-scheduler"
      mv "$SNAPCRAFT_PART_INSTALL/jar/support-scheduler-0.2.1-SNAPSHOT.jar" \
         "$SNAPCRAFT_PART_INSTALL/jar/support-scheduler/support-scheduler.jar"

      # FIXME:
      # copy service license into /usr/share/java/doc, because the
      # jdk plugin has a bug which prevents any files from /usr/share/doc
      # to be staged or primed.
      install -DT "./Attribution.txt" \
         "$SNAPCRAFT_PART_INSTALL/usr/share/java/doc/support-scheduler/Attribution.txt"
      install -DT "./LICENSE-2.0.txt" \
         "$SNAPCRAFT_PART_INSTALL/usr/share/java/doc/support-scheduler/LICENSE-2.0.txt"
    prime:
      - -etc/fonts
      - -etc/fonts/X11
      - -usr/lib/jvm/*/ASSEMBLY_EXCEPTION
      - -usr/lib/jvm/*/THIRD_PARTY_README
      - -usr/lib/jvm/*/jre/ASSEMBLY_EXCEPTION
      - -usr/lib/jvm/*/jre/THIRD_PARTY_README
      - -usr/lib/jvm/*/man
      - -usr/lib/jvm/*/jre/man
      - -usr/lib/jvm/*/jre/lib/images
      - -usr/lib/jvm/*/include
      - -usr/lib/jvm/*/bin
      - -usr/lib/jvm/*/lib
      - -usr/lib/jvm/*/docs
      - -usr/lib/jvm/*/src.zip
      - -usr/share/X11
      - -usr/share/man
      - -usr/share/fonts
      - -usr/share/alsa
  export-client:
    source: https://github.com/edgexfoundry/export-client.git
    source-branch: barcelona
    plugin: maven
    install: |
      echo "Installing export-client files"

      # The logic following logic is all handled by DockerFile for
      # the EdgeX export-client docker image.
      install -d "$SNAPCRAFT_PART_INSTALL/jar/export-client"
      mv "$SNAPCRAFT_PART_INSTALL/jar/export-client-0.2.1-SNAPSHOT.jar" \
         "$SNAPCRAFT_PART_INSTALL/jar/export-client/export-client.jar"

      # FIXME:
      # copy service license into /usr/share/java/doc, because the
      # jdk plugin has a bug which prevents any files from /usr/share/doc
      # to be staged or primed.
      install -DT "./Attribution.txt" \
         "$SNAPCRAFT_PART_INSTALL/usr/share/java/doc/export-client/Attribution.txt"
      install -DT "./LICENSE-2.0.txt" \
         "$SNAPCRAFT_PART_INSTALL/usr/share/java/doc/export-client/LICENSE-2.0.txt"
    prime:
      - -etc/fonts
      - -etc/fonts/X11
      - -usr/lib/jvm/*/ASSEMBLY_EXCEPTION
      - -usr/lib/jvm/*/THIRD_PARTY_README
      - -usr/lib/jvm/*/jre/ASSEMBLY_EXCEPTION
      - -usr/lib/jvm/*/jre/THIRD_PARTY_README
      - -usr/lib/jvm/*/man
      - -usr/lib/jvm/*/jre/man
      - -usr/lib/jvm/*/jre/lib/images
      - -usr/lib/jvm/*/include
      - -usr/lib/jvm/*/bin
      - -usr/lib/jvm/*/lib
      - -usr/lib/jvm/*/docs
      - -usr/lib/jvm/*/src.zip
      - -usr/share/X11
      - -usr/share/man
      - -usr/share/fonts
      - -usr/share/alsa
  export-distro:
    source: https://github.com/edgexfoundry/export-distro.git
#   source-branch: barcelona
    # a unit test failure was fixed post-barcelona
    # however although the version was bumped to 0.5.0,
    # a tag wasn't created.
    source-commit: d2bb39c1f6da463a9b6c8b8b127b0698fc2ef546
    plugin: maven
    prepare: |
      # Remove a broken unit test; for details see:
      # https://github.com/edgexfoundry/export-distro/issues/40
      rm "./src/test/java/org/edgexfoundry/messaging/MQTTSenderTest.java"
    install: |
      echo "Installing export-distro files"

      # The logic following logic is all handled by DockerFile for
      # the EdgeX export-distro docker image.
      install -d "$SNAPCRAFT_PART_INSTALL/jar/export-distro"
      mv "$SNAPCRAFT_PART_INSTALL/jar/export-distro-0.5.0-SNAPSHOT.jar" \
         "$SNAPCRAFT_PART_INSTALL/jar/export-distro/export-distro.jar"

      # FIXME:
      # copy service license into /usr/share/java/doc, because the
      # jdk plugin has a bug which prevents any files from /usr/share/doc
      # to be staged or primed.
      install -DT "./Attribution.txt" \
         "$SNAPCRAFT_PART_INSTALL/usr/share/java/doc/export-distro/Attribution.txt"
      install -DT "./LICENSE-2.0.txt" \
         "$SNAPCRAFT_PART_INSTALL/usr/share/java/doc/export-distro/LICENSE-2.0.txt"
    prime:
      - -etc/fonts
      - -etc/fonts/X11
      - -usr/lib/jvm/*/ASSEMBLY_EXCEPTION
      - -usr/lib/jvm/*/THIRD_PARTY_README
      - -usr/lib/jvm/*/jre/ASSEMBLY_EXCEPTION
      - -usr/lib/jvm/*/jre/THIRD_PARTY_README
      - -usr/lib/jvm/*/man
      - -usr/lib/jvm/*/jre/man
      - -usr/lib/jvm/*/jre/lib/images
      - -usr/lib/jvm/*/include
      - -usr/lib/jvm/*/bin
      - -usr/lib/jvm/*/lib
      - -usr/lib/jvm/*/docs
      - -usr/lib/jvm/*/src.zip
      - -usr/share/X11
      - -usr/share/man
      - -usr/share/fonts
      - -usr/share/alsa
  support-rulesengine:
    source: https://github.com/edgexfoundry/support-rulesengine.git
    source-branch: barcelona
    plugin: maven
    install: |
      echo "Installing support-rulesengine files"

      # The logic following logic is all handled by DockerFile for
      # the EdgeX support-rulesengine docker image.
      install -d "$SNAPCRAFT_PART_INSTALL/jar/support-rulesengine"
      mv "$SNAPCRAFT_PART_INSTALL/jar/support-rulesengine-0.2.1-SNAPSHOT.jar" \
         "$SNAPCRAFT_PART_INSTALL/jar/support-rulesengine/support-rulesengine.jar"

      # FIXME:
      # copy service license into /usr/share/java/doc, because the
      # jdk plugin has a bug which prevents any files from /usr/share/doc
      # to be staged or primed.
      install -DT "./Attribution.txt" \
         "$SNAPCRAFT_PART_INSTALL/usr/share/java/doc/support-rulesengine/Attribution.txt"
      install -DT "./LICENSE-2.0.txt" \
         "$SNAPCRAFT_PART_INSTALL/usr/share/java/doc/support-rulesengine/LICENSE-2.0.txt"
    prime:
      - -etc/fonts
      - -etc/fonts/X11
      - -usr/lib/jvm/*/ASSEMBLY_EXCEPTION
      - -usr/lib/jvm/*/THIRD_PARTY_README
      - -usr/lib/jvm/*/jre/ASSEMBLY_EXCEPTION
      - -usr/lib/jvm/*/jre/THIRD_PARTY_README
      - -usr/lib/jvm/*/man
      - -usr/lib/jvm/*/jre/man
      - -usr/lib/jvm/*/jre/lib/images
      - -usr/lib/jvm/*/include
      - -usr/lib/jvm/*/bin
      - -usr/lib/jvm/*/lib
      - -usr/lib/jvm/*/docs
      - -usr/lib/jvm/*/src.zip
      - -usr/share/X11
      - -usr/share/man
      - -usr/share/fonts
      - -usr/share/alsa
  device-virtual:
    source: https://github.com/edgexfoundry/device-virtual.git
    source-branch: barcelona
    plugin: maven
    install: |
      echo "Installing device-virtual files"

      # The logic following logic is all handled by DockerFile for
      # the EdgeX device-virtual docker image.
      install -d "$SNAPCRAFT_PART_INSTALL/jar/device-virtual"
      install -d "$SNAPCRAFT_PART_INSTALL/jar/device-virtual/bacnet_sample_profiles"
      install -d "$SNAPCRAFT_PART_INSTALL/jar/device-virtual/modbus_sample_profiles"
      mv "$SNAPCRAFT_PART_INSTALL/jar/device-virtual-0.2.1-SNAPSHOT.jar" \
         "$SNAPCRAFT_PART_INSTALL/jar/device-virtual/device-virtual.jar"
      cp ./bacnet_sample_profiles/*.yaml \
         "$SNAPCRAFT_PART_INSTALL/jar/device-virtual/bacnet_sample_profiles/"
      cp ./modbus_sample_profiles/*.yaml \
         "$SNAPCRAFT_PART_INSTALL/jar/device-virtual/modbus_sample_profiles/"

      # FIXME:
      # copy service license into /usr/share/java/doc, because the
      # jdk plugin has a bug which prevents any files from /usr/share/doc
      # to be staged or primed.
      install -DT "./Attribution.txt" \
         "$SNAPCRAFT_PART_INSTALL/usr/share/java/doc/device-virtual/Attribution.txt"
      install -DT "./LICENSE-2.0.txt" \
         "$SNAPCRAFT_PART_INSTALL/usr/share/java/doc/device-virtual/LICENSE-2.0.txt"
    prime:
      - -etc/fonts
      - -etc/fonts/X11
      - -usr/lib/jvm/*/ASSEMBLY_EXCEPTION
      - -usr/lib/jvm/*/THIRD_PARTY_README
      - -usr/lib/jvm/*/jre/ASSEMBLY_EXCEPTION
      - -usr/lib/jvm/*/jre/THIRD_PARTY_README
      - -usr/lib/jvm/*/man
      - -usr/lib/jvm/*/jre/man
      - -usr/lib/jvm/*/jre/lib/images
      - -usr/lib/jvm/*/include
      - -usr/lib/jvm/*/bin
      - -usr/lib/jvm/*/lib
      - -usr/lib/jvm/*/docs
      - -usr/lib/jvm/*/src.zip
      - -usr/share/X11
      - -usr/share/man
      - -usr/share/fonts
      - -usr/share/alsa
